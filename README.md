# Random Network Lab

Вебзастосунок для **моделювання, аналізу та дослідження випадкових і реальних мереж**  
з використанням **NetworkX**, **Flask** та інтерактивної візуалізації через **D3.js**.

Проєкт дозволяє:
- генерувати випадкові мережі (Erdős–Rényi, Watts–Strogatz, Barabási–Albert);
- аналізувати реальні мережі з файлів (.txt / .csv / .edgelist);
- працювати з популярними науковими мережами (SNAP, Wikipedia, email networks);
- обчислювати структурні метрики, центральності та кореляції;
- зберігати результати досліджень та мережі для подальшого перегляду.

---

## 🧪 Функціональні можливості

- **Одиничний режим** — аналіз однієї мережі  
- **Груповий режим** — статистика по набору згенерованих мереж  
- Метрики: кластеризація, транзитивність, діаметр, центральності, асортативність  
- Кореляції між центральностями та між метриками  
- Інтерактивна візуалізація графа (D3.js)  
- Збереження:
  - метаданих у PostgreSQL
  - структури мережі у MongoDB  
- Експорт результатів у JSON та edge list

---

## 🛠 Технології

- Python 3.11
- Flask (через ASGI + Uvicorn)
- NetworkX
- NumPy
- PostgreSQL
- MongoDB
- D3.js
- Docker & Docker Compose

---

##  Швидкий запуск через Docker 

### Клонування репозиторію
```bash
git clone https://github.com/your-username/random-network-lab.git
cd random-network-lab
```
Створення .env

```bash
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=netlab
POSTGRES_USER=netlab
POSTGRES_PASSWORD=netlab

MONGODB_HOST=mongo
MONGODB_PORT=27017
MONGODB_DB=random_netlab
```


```bash
docker compose up --build
```

Відкрити в браузері
http://localhost:8000

# Локальний запуск

## Створення віртуального середовища
```bash
python3.11 -m venv .venv
source .venv/bin/activate
```

Встановлення залежностей
```bash
pip install -r requirements.txt
```
## Налаштування змінних середовища
#### Створіть .env у корені проєкту:
```bash
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=netlab
POSTGRES_USER=YOUR_USER
POSTGRES_PASSWORD=YOUR_PASSWORD

MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_DB=random_netlab

SECRET_KEY=dev-secret
```

Запуск
```bash
uvicorn asgi:asgi_app --reload
```
